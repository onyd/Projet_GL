class Car {
    protected int maxSpeed;

    void Car() {
        this.maxSpeed = max;
    }

    void setMaxSpeed(int newSpeed) {
        maxSpeed = newSpeed;
    }

    int getMaxSpeed(int newSpeed) {
            return maxSpeed;
    }
}